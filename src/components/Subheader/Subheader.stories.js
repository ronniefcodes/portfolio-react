import React from 'react';
import { storiesOf, } from '@storybook/react';
import Subheader from 'Components/Subheader';

storiesOf('Styled Components', module)
  .add('Subheader', () => <Subheader>This is a subheader</Subheader>);
